(function() {
  var ReadForMe;

  ReadForMe = (function() {
    function ReadForMe(config) {
      this.title = config.title;
      this.article = config.article;
      this.lang = config.lang;
    }

    return ReadForMe;

  })();

}).call(this);
