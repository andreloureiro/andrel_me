nav = document.querySelector 'nav'

# nav.style.marginTop = "#{(window.innerHeight / 2) - (nav.offsetHeight / 2)}px"

# window.addEventListener 'resize', ->
# 	nav.style.top = "#{(window.innerHeight / 2) - (nav.offsetHeight / 2)}px"